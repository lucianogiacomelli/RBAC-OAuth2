spring.application.name=RBAC-OAuth2

# Base de datos (referenciando variables)
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# ==========================================
# Configuración de Auth0 (OAuth2 Resource Server)
# ==========================================

spring.security.oauth2.resourceserver.jwt.issuer-uri=${AUTH0_ISSUER_URI}
spring.security.oauth2.resourceserver.jwt.audiences=${AUTH0_AUDIENCE}

# Dominio de Auth0
auth0.domain=${AUTH0_DOMAIN}

# Credenciales M2M
auth0.m2m.client-id=${AUTH0_CLIENT}
auth0.m2m.client-secret=${AUTH0_CLIENT_SECRET}